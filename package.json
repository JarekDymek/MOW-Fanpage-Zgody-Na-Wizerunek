{
  "name": "mow-fanpage-zgody",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm test && vite build",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
